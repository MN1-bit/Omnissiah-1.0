# Part 5 - Step 9: 메인 컨트롤러 통합 (하이브리드 업데이트)

## 상태: ✅ 완료 (하이브리드 방식 적용 예정)

## 목표
모든 모듈을 main.py에 통합 + 하이브리드 업데이트 방식

## 동작 순서 (하이브리드)

### 기본 루프 (5초 간격)
```
1. 킬 스위치 체크 (가장 먼저!)
2. GUI 킬 스위치 상태 업데이트
3. if kill_status != "CLEAR": black_mode 실행 → return
4. 일봉 통계 캐시 갱신 (1일 1회)
5. Z-Score 계산 (캐시된 통계 + 현재 VIX)
6. 레짐 판단
7. 해당 전략 실행
```

### 이벤트 트리거 (즉시)
- VIX ±0.5pt 변동 시 → Z-Score 재계산
- |Z-Score| ≥ 1.0 시 → 루프 주기 1초로 전환

## 구현 항목
- [x] main.py 전면 수정
- [x] 모든 모듈 import 및 연결
- [ ] **하이브리드 타이머** (5초 기본 + 이벤트)
- [ ] **동적 주기 전환** (Z-Score 임계값)
- [x] 레짐별 전략 분기

## 핵심 규칙
- ⚠️ time.sleep() 절대 금지
- ⚠️ 킬 스위치가 항상 1순위
- ⚠️ 일봉 통계는 캐싱 (1일 1회)
- ⚠️ 실시간 VIX만 자주 업데이트

## 완료 조건
- [x] python main.py 실행 시 전체 시스템 가동
- [x] GUI에서 레짐/상태 실시간 표시
- [ ] 하이브리드 업데이트 동작
