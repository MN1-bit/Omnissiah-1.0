# Part 6 - Step 12.5: 적응형 오버나이트 전략

## 상태: 🟡 진행 중

## 목표
고정값 청산 → 적응형 오버나이트 판단 로직으로 개선

## 배경
- 기존: 15:50 무조건 청산 (횡보), MA20만 체크 (상승)
- 개선: 시장 상황에 따라 오버나이트 킵 결정

## 적응형 파라미터 (고정값 X)

| 판단 항목 | 고정값 | 적응형 |
|----------|--------|--------|
| VIX 위험 | 25 | vix_mean + vix_std |
| 과열 판단 | +3% | daily_return > ATR × 2 |
| 목표 근접 | 0.5% | daily_range × 0.5 |

## 수정 파일

### 1. strategy/green_mode.py
- [x] `should_keep_overnight()` 추가
- [x] 시간대 US/Eastern 통일

### 2. strategy/red_mode.py
- [x] `should_keep_overnight()` 추가
- [x] ATR 기반 판단

### 3. core/scheduler.py
- [x] `get_close_action(regime, context)` 수정

### 4. core/market_data.py
- [x] `get_atr()` 추가
- [x] `get_vix_stats()` 추가

### 5. main.py
- [x] `_handle_pre_close()` 컨텍스트 전달

## 완료 조건
- 적응형 파라미터로 오버나이트 결정
- 시간대 통일 (US/Eastern)
- 테스트 통과
